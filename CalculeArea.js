let base = prompt ("Insira a base")
let altura = prompt ("Insira a altura")

function calculaArea(base, altura) { 
area = base * altura 
return area; 
}

alert ("Sua área é de: " + calculaArea(base, altura)) 